inicio = 3;
fin = 6;

// Crear arreglo con números del rango
let arreglo = [];
for (let i = inicio; i <= fin; i++) {
  arreglo.push(i);
}

// Imprimir arreglo
console.log("Arreglo de números del rango:");
console.log(arreglo);